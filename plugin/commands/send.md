Send a message to the shared room: $ARGUMENTS

Check the project CLAUDE.md or AGENTS.md for the room ID and your assigned username, then run:

```bash
room send <room-id> <username> "$ARGUMENTS"
```

The command exits after delivery and prints the broadcast JSON confirmation.
