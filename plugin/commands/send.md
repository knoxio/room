Send a message to the shared room: $ARGUMENTS

Check the project CLAUDE.md or AGENTS.md for the room ID and your session token, then run:

```bash
room send <room-id> --token <token> "$ARGUMENTS"
```

The command exits after delivery and prints the broadcast JSON confirmation.
